# 🏗️ 双机 Docker 部署架构说明

## 架构概览

```text
┌─────────────────────────────────────────────────────────────────┐
│                         开发与部署流程                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   Mac mini M4 (开发机)                                          │
│   ┌──────────────────┐                                         │
│   │  编写业务代码      │                                         │
│   │  创建 Dockerfile  │──────┐                                  │
│   │  本地测试          │      │                                  │
│   └──────────────────┘      │                                  │
│                             ▼                                  │
│                      ┌─────────────┐                           │
│                      │   GitHub    │                           │
│                      └─────────────┘                           │
│                             │                                  │
│                             ▼                                  │
│   Mac Air M4 / VPS (服务器)                                     │
│   ┌──────────────────────────────────────────┐                 │
│   │  Infrastructure/                          │                 │
│   │  ├── docker-compose.yml  ◄─── 编排中心    │                 │
│   │  ├── .env               ◄─── 密钥配置     │                 │
│   │  └── update.sh          ◄─── 一键更新     │                 │
│   └──────────────────────────────────────────┘                 │
│                             │                                  │
│                             ▼                                  │
│                    ┌─────────────────┐                         │
│                    │  Docker 容器运行  │                         │
│                    │  :8001 :8002 ... │                         │
│                    └─────────────────┘                         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 目录结构

### 服务器端 (Mac Air / VPS)

```text
~/Projects/
├── Infrastructure/          # 总控工程
│   ├── docker-compose.yml
│   ├── .env
│   └── update.sh
│
├── Demo-Backend/            # 业务项目（由 Git 同步）
├── User-Backend/
└── ...
```

### 开发端 (Mac mini)

```text
~/Projects/
├── Demo-Backend/            # 业务项目
│   ├── main.py
│   ├── Dockerfile
│   └── ...
└── ...
```

---

## 端口规划

| 端口范围 | 用途 |
|---------|------|
| 8001-8049 | 核心业务服务 |
| 8050-8079 | 工具/辅助服务 |
| 8080-8099 | 测试/临时服务 |

---

## 工作流程

1. **Mac mini**: 编写代码 → 创建 Dockerfile → `git push`
2. **Mac Air**: 运行 `./update.sh` → 自动拉取 + 重建容器
3. **访问服务**: `http://localhost:8001` (或对应端口)
